<Window x:Class="PRN212_Group4.TransactionDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PRN212_Group4"
        mc:Ignorable="d"
        Title="TransactionDetailWindow" Height="450" Width="800" Background="#FF99CC" FontFamily="Arial" FontSize="14">
    <Grid>
        <Label x:Name="lbSearch" Content="Order Id:" HorizontalAlignment="Left" Margin="73,76,0,0" VerticalAlignment="Top" Height="32" Width="86" FontWeight="Bold"/>
        <TextBox x:Name="txtSearch" HorizontalAlignment="Left" Height="32" Margin="147,73,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="432"/>
        <Button x:Name="btnSearch" Content="Search" HorizontalAlignment="Left" Margin="597,73,0,0" VerticalAlignment="Top" Height="32" Width="62" Click="btnSearch_Click"/>
        <DataGrid x:Name="dgUserOrders" Margin="28,127,28,104" AutoGenerateColumns="False" SelectionChanged="dgUserOrders_SelectionChanged" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Transaction Id" Binding="{Binding Transactionid}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Order id" Binding="{Binding Orderid}" Width="90"/>
                <DataGridTextColumn Header="Product Name" Binding="{Binding ProductTitle}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding OrderPrice}" Width="*"/>
                <DataGridTextColumn Header="Order Status" Binding="{Binding OrderStatus}" Width="*"/>

            </DataGrid.Columns>
        </DataGrid>
        <Label x:Name="lbtransactiondetail" Content="Transaction Detail" HorizontalAlignment="Left" Height="43" Margin="113,10,0,0" VerticalAlignment="Top" Width="533" FontWeight="Bold" FontSize="20" HorizontalContentAlignment="Center" />


    </Grid>
</Window>
