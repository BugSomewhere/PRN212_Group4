<Window x:Class="PRN212_Group4.CartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Giỏ hàng" Height="400" Width="600"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <DataGrid x:Name="cartGrid" CanUserAddRows="False" AutoGenerateColumns="False" Margin="0,0,0,60">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding Product.Title}" Width="*"/>
                <DataGridTextColumn Header="Số lượng" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Đơn giá" Binding="{Binding Product.Price}" Width="100"/>
                <DataGridTextColumn Header="Thành tiền" Binding="{Binding TotalPrice}" Width="120"/>
                <DataGridTemplateColumn Header="Xóa" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="❌"
                            Width="40"
                            Height="25"
                            Background="LightCoral"
                            Foreground="White"
                            Click="RemoveItem_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <TextBlock Text="Tổng cộng: " VerticalAlignment="Center"/>
            <TextBlock x:Name="txtTotal" FontWeight="Bold" Margin="5,0,15,0" VerticalAlignment="Center"/>
            <Button Content="Thanh toán" Width="120" Click="Checkout_Click"/>
        </StackPanel>
    </Grid>
</Window>
